<h4>This is the index view we are using the instance variable of @articles</h4>

<%= link_to "Create New", new_article_path %>

<% @articles.each do |article| %>
    <h4><%= link_to article.title, article %></h4>
    <p>Author: <%= article.author %>  | Rating: <%= article.rating %></p>
    <p><%= article.body %></p>
<% end %>

